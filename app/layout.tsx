import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "OmniBuild AI - AI-Powered Green Building Intelligence",
  description: "Automate green building compliance, visualize carbon data in 3D, and optimize for sustainability with AI. Built for the China-ASEAN construction corridor.",
  keywords: ["green building", "BIM", "sustainability", "AI", "compliance", "LEED", "GBL", "GBI", "carbon footprint"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
